#include <iostream>

int main() {

    using namespace std;

    // vars: variables
    string name = "John"; // no need to use std::string name = "John";
    int age = 35;
    double gpa = 3.5;
    char grade = 'A';

    cout << name << " - age: " << age << " - pga: " << gpa << " - grade: " << grade << endl;

    return 0;
}